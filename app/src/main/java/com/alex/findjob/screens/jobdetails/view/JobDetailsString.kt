package com.alex.findjob.screens.jobdetails.view

import com.alex.findjob.localization.Translatable
import java.util.Locale

val salary = Translatable(
    "Зарплата",
    hashMapOf(
        Locale.ENGLISH to "Salary"
    )
)

val location = Translatable(
    "Местоположение",
    hashMapOf(
        Locale.ENGLISH to "Location"
    )
)

val description = Translatable(
    "Описание",
    hashMapOf(
        Locale.ENGLISH to "Description"
    )
)

val postedOn = Translatable(
    "Опубликовано ",
    hashMapOf(
        Locale.ENGLISH to "Posted on "
    )
)

