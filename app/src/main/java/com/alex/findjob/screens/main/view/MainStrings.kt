package com.alex.findjob.screens.main.view

import com.alex.findjob.localization.Translatable
import java.util.Locale

val hello = Translatable(
    "Привет, ",
    hashMapOf(
        Locale.ENGLISH to "Hello, "
    )
)

val search = Translatable(
    "Поиск",
    hashMapOf(
        Locale.ENGLISH to "Search"
    )
)

val selectCountry = Translatable(
    "Выберите страну",
    hashMapOf(
        Locale.ENGLISH to "Select country"
    )
)

val location = Translatable(
    "Местоположение",
    hashMapOf(
        Locale.ENGLISH to "Location"
    )
)

val job = Translatable(
    "Вакансия",
    hashMapOf(
        Locale.ENGLISH to "Job"
    )
)

val jobType = Translatable(
    "Вид работы",
    hashMapOf(
        Locale.ENGLISH to "Job type"
    )
)

val fullTime = Translatable(
    "Полный день",
    hashMapOf(
        Locale.ENGLISH to "Full time"
    )
)

val partTime = Translatable(
    "Неполный день",
    hashMapOf(
        Locale.ENGLISH to "Part time"
    )
)

val nothingHere = Translatable(
    "Ничего нет, попробуйте изменить запрос...",
    hashMapOf(
        Locale.ENGLISH to "Nothing here, please change request..."
    )
)

val salaryIsNotSpecified = Translatable(
    "Зарплата не указана",
    hashMapOf(
        Locale.ENGLISH to "Salary is not specified"
    )
)
