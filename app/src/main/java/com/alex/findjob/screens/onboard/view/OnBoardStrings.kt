package com.alex.findjob.screens.onboard.view

import com.alex.findjob.localization.Translatable
import java.util.Locale

val next = Translatable(
    "Продолжить",
    hashMapOf(
        Locale.ENGLISH to "Next"
    )
)

val finish = Translatable(
    "Завершить",
    hashMapOf(
        Locale.ENGLISH to "Get Started"
    )
)

val onBoard1MainText = Translatable(
    "Здесь работа ищет тебя",
    hashMapOf(
        Locale.ENGLISH to "The place where work finds you"
    )
)

val onBoard2MainText = Translatable(
    "Начните вашу карьеру здесь",
    hashMapOf(
        Locale.ENGLISH to "Let's start your career here now!"
    )
)

val onBoard3MainText = Translatable(
    "Мы находим, Вы выбираете",
    hashMapOf(
        Locale.ENGLISH to "We find, you choose"
    )
)
