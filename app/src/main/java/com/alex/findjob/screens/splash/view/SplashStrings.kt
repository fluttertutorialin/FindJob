package com.alex.findjob.screens.splash.view

import com.alex.findjob.localization.Translatable
import java.util.Locale

val enterYourName = Translatable(
    "Введите ваше имя...",
    hashMapOf(
        Locale.ENGLISH to "Enter your name..."
    )
)

val next = Translatable(
    "Далее",
    hashMapOf(
        Locale.ENGLISH to "Next"
    )
)
